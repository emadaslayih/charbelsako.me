const initialState = [
  {
    title: 'Front end developer',
    startDate: 'July 2019',
    endDate: 'Sep 2019'
  }
];

export default function experienceReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
